package com.example.command.adapter.repository.predict_cost;

import com.example.command.domain.predict_cost.PredictCost;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PredictCostRepository extends JpaRepository<PredictCost, Long> {
}
